package JavaProject.MoneyWise.models.enums;

public enum GroupRole {
    MEMBER,        // maps to “Member = 0”
    COLLABORATOR,  // maps to “Collaborator = 1”
    ADMIN          // maps to “Admin = 2”
}
